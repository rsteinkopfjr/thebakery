<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Full Product Information</h5>
          <div>
            <p>
              <strong>ID</strong>: {{example.id}}</p>
            <p>
              <strong>Name</strong>: {{example.text}}</p>
            <p>
              <strong>Category</strong>: {{example.category}}</p>
            <p>
              <strong>Description:</strong> {{example.description}}</p>
            <p>
              <strong>Price</strong>: {{example.price}}</p>
            <a href="/products">← Back To Products</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>